{
  "name": "visual-data-builder-prototype",
  "version": "1.0.0",
  "description": "Visual Data Builder - Interactive Prototype",
  "scripts": {
    "dev": "live-server --port=3000 --no-browser",
    "start": "live-server --port=3000"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
